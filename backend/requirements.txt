# Skillora Backend — runtime dependencies (latest stable).
#   Install:  python -m pip install -r requirements.txt
#   Run:      python -m app.init_db && uvicorn app.main:app --host 127.0.0.1 --port 8001
#
# The ML Gateway runs as a separate service on :8000 (see ../ml/requirements.txt).
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.52.4
pydantic==2.13.4
SQLAlchemy==2.0.51
PyJWT==2.13.0
httpx==0.28.1
python-dotenv==1.2.2
# Production database driver (SQLAlchemy DATABASE_URL switches to postgresql+psycopg2)
psycopg2-binary==2.9.10
