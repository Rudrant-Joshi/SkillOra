# Core service layer
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
PyYAML>=6.0

# LLM provider
anthropic>=0.34

# Embeddings (optional — falls back to a deterministic local hashing
# embedder in shared/model_router.py if not installed, so the pipeline
# still runs offline without this dependency)
sentence-transformers>=3.0

# Testing
pytest>=8.0
httpx>=0.27
