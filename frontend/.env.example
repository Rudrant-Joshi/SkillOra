# Used by the Vite build (baked into the JS bundle).
# In Docker this is passed as a build ARG (VITE_API_URL, default /api).
# For local dev without Docker:  VITE_API_URL=http://localhost:8001 npm run dev
VITE_API_URL=http://localhost:8001
